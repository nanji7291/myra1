name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Unzip Source Code
        run: |
          echo "Unzipping project file..."
          unzip -o Laprri.ai-assistant.zip -d extracted_project

      - name: Build Debug APK
        run: |
          # Extracted folder ke andar build.gradle dhoondho
          BUILD_FILE=$(find extracted_project -name "build.gradle" -o -name "build.gradle.kts" | head -n 1)
          
          if [ -z "$BUILD_FILE" ]; then
            echo "Error: Zip file ke andar build.gradle nahi mila!"
            exit 1
          fi

          PROJECT_DIR=$(dirname "$BUILD_FILE")
          echo "Project directory found: $PROJECT_DIR"
          cd "$PROJECT_DIR"

          # Agar gradlew nahi hai toh gradle command se wrapper generate karega
          if [ ! -f "gradlew" ]; then
            echo "gradlew missing tha, generate kiya ja raha hai..."
            gradle wrapper
          fi

          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-app-apk
          path: '**/build/outputs/apk/debug/*.apk'
